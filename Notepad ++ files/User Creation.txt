sudo adduser newuser  ( For linux)
sudo adduser newuser --disabled-password
sudo chage -M 180 username
sudo chmod -c " comment with team member" username
sudo usermod --shell /bin/bash username ( To provide the shell )
sudo su - username
mkdir .ssh
chmod 700 .ssh
touch .ssh/authorized_keys
chmod 600 .ssh/authorized_keys
ssh-keygen ( to generate the public and private key )
cp id_rsa.pub authorized_keys ( copy the public key to authorized_keys )
cat id_rsa and copy the private key and saved to our desktop to share with user.
Add the user under /etc/sudoers list like below
username ALL= (ALL) NOPASSWD: ALL